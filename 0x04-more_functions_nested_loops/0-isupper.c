#include "main.h"

/**
 * _isupper - checks for uppercase character
 * @c: the character
 * Return: 1 if letter is uppercase, 0 if not
 */
int _isupper(int c)
{
	if (C >= 'A' && C <= 'Z')
		return (1);
	else
		return (0);
}
